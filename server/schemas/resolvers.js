// Sample placeholder resolver.js file

const resolvers = {

    // You can customize this function to handle different placeholders
    // For now, it simply returns the placeholder name as the resolved value
};
  
export default resolvers;
  